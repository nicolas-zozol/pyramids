{
  "name": "@robusta/pyramids-routing",
  "type": "module",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "lint": "eslint src/**/*.ts",
    "test": "vitest run",
    "watch": "tsc -p . --watch"
  },
  "devDependencies": {
    "typescript": "^5",
    "vitest": "^3.0.4"
  }
}
